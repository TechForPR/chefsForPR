<div class="container-fluid">
    <div class="jumbotron">
        <h1>{{title}}</h1>
        <p>{{{message}}}</p>
    </div>
    <form class="form">
        {{{ form }}}
        <button class="btn btn-primary" type="button" >{{submitText}}</button>
    </form>
</div>
